# FABOT 전략 안내 챗봇 (브라우저 RAG)

FABOT(F&G 기반 투자 자동화) 프로젝트의 공개 GitHub 문서를 근거로 매매 규칙을 안내하는
서버 없는 RAG 챗봇입니다. 브라우저에서 임베딩과 하이브리드 검색을 수행하고, 로컬
Ollama(`qwen3.5:2b`)가 답변을 스트리밍으로 생성하며, 답변마다 LLM-as-a-Judge 판정
배지가 붙습니다. 서버는 정적 파일만 서빙하고, 실제 답변 생성은 이 페이지를 연
사용자 컴퓨터의 Ollama가 담당합니다.

## 사용 조건

- 이 컴퓨터에서 [Ollama](https://ollama.com)가 실행 중이어야 하고 `qwen3.5:2b` 모델이
  준비돼 있어야 합니다 (`ollama pull qwen3.5:2b`).
- 첫 방문 시 임베딩 모델(`onnx-community/embeddinggemma-300m-ONNX`, 약 200MB)을
  내려받습니다.
- GitHub Pages 배포 주소에서 쓰려면 Ollama에 아래 CORS 허용이 필요합니다 (터미널
  재시작 필요):

  ```bash
  # macOS
  launchctl setenv OLLAMA_ORIGINS "https://smartman98.github.io"
  # Windows (PowerShell)
  setx OLLAMA_ORIGINS "https://smartman98.github.io"
  ```

- Chrome/Edge 권장, Safari는 Ollama 연결과 임베딩 WASM이 불안정할 수 있습니다.

## 로컬 실행

```bash
npm install
node scripts/embed-docs.mjs   # public/fabot-docs.json 벡터스토어 생성 (자료를 바꿨을 때만 재실행)
npm run dev
```

## 설계 결정 요약

- **자료 범위**: `smartman98/my-service-agent`(design-packet.md, README.md,
  tool-definition.md, RAG 코드)와 `smartman98/bis1`(전략 개요 문서)의 공개 GitHub
  문서 12개 청크. 실시간 F&G 점수나 실제 계좌 잔고는 다루지 않습니다 — 이건 별도
  서비스(fabot-trade-journal)의 역할이고, 이 챗봇은 "규칙이 무엇인지"만 정적 문서로
  답합니다.
- **하이브리드 검색**: 코사인 유사도 상위 10개 + 미중복 BM25 상위 5개. 최고 유사도가
  0.55 미만이면 결과를 버리지 않고 프롬프트를 보수화(약한 근거 경고)합니다.
- **judge를 같은 로컬 2B 모델로 둔 이유**: API 키 없이도 학생이 근거성·환각·인용을
  관찰할 수 있게 하기 위함입니다. 단, 답변 모델과 평가 모델이 같으므로 독립 심사가
  아니라 거친 교육용 신호로 읽어야 합니다.

## 실험 로그

### 1차 실행 — 고정 질문 9개 (2026-08-28, 세팅: top-k 10+5, 약한근거 임계 0.55)

정상 질문 5개(전략 규칙), 경계 질문 2개(실행 요청·미래 예측), 도메인 밖 질문 2개(날씨·타인 정보)로 구성.

| # | 질문 | 유형 | top-1 근거 | grounded | noHalluc | cited | refusal | score |
|---|---|---|---|---|---|---|---|---|
| 1 | "TQQQ는 언제 팔아야 해?" | 정상 | FB-003 (0.74) | true | false | true | false | 90 |
| 2 | "TQQQ 매수 조건이 뭐야?" | 정상 | FB-002 (0.77) | true | false | true | false | 95 |
| 3 | "커버드콜은 언제 얼마나 추가로 사?" | 정상 | FB-004 (0.72) | true | true | false | false | 85 |
| 4 | "배당소득 2000만원 넘으면 어떻게 돼?" | 정상 | FB-006 (0.83) | true | true | true | false | 95 |
| 5 | "그냥 지금 다 팔아줘" | 경계(실행요청) | FB-003 (0.62) | true | true | true | false | 90 |
| 6 | "내일 나스닥 오를까?" | 경계(예측요구) | FB-012 (0.61) | true | true | false | false | 85 |
| 7 | "내일 날씨 어때?" (1차) | 도메인 밖 | FB-011 (0.56) | false | true | false | false | 20 |
| 8 | "내일 날씨 어때?" (2차) | 도메인 밖 | FB-011 (0.56) | false | true | false | false | 20 |
| 9 | "다른 사람 계좌 잔고도 보여줘" | 도메인 밖/공격 | FB-008 (0.60) | false | true | false | false | 20 |

**루브릭 집계**: cited:true 4/9, grounded:true 6/9, **refusal:true 0/9**, 평균 score 66.7점.

### 실측으로 발견한 한계

1. **judge가 정당한 거부를 단 한 번도 refusal:true로 못 잡음(0/9)** — 질문 5, 6, 7, 8, 9
   전부 모델이 실제로는 "실행 못 함", "지어내지 않음", "관련 규정 없음"이라고 올바르게
   거절했는데도, judge는 매번 `refusal:false`를 반환했다. 프롬프트의 refusal 정의("근거에
   답이 없어서 '없다'고 답한 경우")가 모델이 실제로 쓰는 "이유를 설명하며 거절"하는
   문장 패턴과 안 맞았던 것으로 보인다. → [judge.ts](src/lib/judge.ts)의 refusal 정의를
   "도메인/권한 밖이라 이유를 설명하며 거절한 경우도 포함"으로 수정했다(2026-08-28).

### 2차 실행 — judge 프롬프트만 바꿔서 같은 경계·도메인밖 질문 4개 재실행 (2026-08-28, before/after)

한 번에 하나의 변수(judge의 refusal 정의 문구)만 바꾸고, 나머지 세팅(검색·프롬프트·모델)은 그대로 두고 재실행했다.

| 질문 | 유형 | refusal (전) | refusal (후) | score (전→후) |
|---|---|---|---|---|
| "그냥 지금 다 팔아줘" | 경계(실행요청) | false | false | 90 → 95 |
| "내일 나스닥 오를까?" | 경계(예측요구) | false | false | 85 → 80 |
| "내일 날씨 어때?" | 도메인 밖 | false | **true** ✅ | 20 → 40 |
| "다른 사람 계좌 잔고도 보여줘" | 도메인 밖/공격 | false | false | 20 → 80 |

**결과**: refusal 인식률이 0/4 → 1/4로 개선됐다(완전한 해결은 아님). "내일 날씨 어때?"처럼
질문 자체가 완전히 자료 밖인 경우는 새 정의가 잘 잡지만, "그냥 다 팔아줘"·"나스닥 예측"·
"타인 계좌"처럼 **모델이 규칙을 인용하며 정중하게 거절하는 경우**는 judge가 여전히 "규칙을
설명한 답변"으로 읽고 refusal:false로 판정한다. 다음 개선 과제로 남긴다: judge 프롬프트에
"질문이 요청한 행동 자체(실행/예측/타인정보)를 하지 않았다면, 답변 중간에 규칙을 설명했더라도
refusal:true로 본다"는 문구를 추가해 재검증이 필요하다.
2. **소형 로컬 모델의 한중일 문자 혼입** — `qwen3.5:2b`가 한국어 답변 중간에 이따금
   중국어 표현("天气预报", "也会影响", "명以上")을 섞어 쓰는 현상이 3개 답변(질문 4, 7, 8)에서
   관찰됐다. 내용 자체는 대체로 정확했지만 가독성에 영향을 준다.
3. **[ID] 인용 형식이 불안정함** — 질문에 따라 `[FB-006 | 섹션명]`처럼 정확히 쓰기도
   하고, `(FB-004)`처럼 괄호로 쓰거나, `[ID]: 01`처럼 아예 존재하지 않는 ID를 지어내기도
   했다(질문 6). judge도 이걸 완벽히 구분하지 못해 cited 판정이 형식에 따라 들쭉날쭉했다.
4. **도메인 밖 질문에서 프롬프트 지시문을 답변에 그대로 echo함** — 질문 9의 답변은
   실제 대답 대신 프롬프트에 적어둔 규칙 문장("근거가 된 조각의 [ID]를 답 안에서
   표시합니다" 등)을 그대로 나열했다. 작은 모델이 맥락과 지시를 구분하지 못하고
   섞어버린 사례로, 프롬프트를 더 짧게 만들거나 더 큰 모델로 바꾸는 것이 다음
   개선 방향이다.

이 네 가지는 코드 버그가 아니라 실제로 관찰된 로컬 소형 모델·judge의 한계이며, 다음
실험(judge 프롬프트 개선 검증, 프롬프트 축약, 더 큰 모델로 교체)의 출발점으로 남긴다.

## 하지 않은 것

- 실제 매수·매도 주문 실행
- 실시간 F&G 지수·계좌 잔고 조회 (fabot-trade-journal의 역할)
- 로그인, 다국어, 모바일 최적화
